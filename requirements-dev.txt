# Dev/test dependencies — not needed in production containers
# Install with: pip install -r requirements-dev.txt
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.27.2          # ASGI test client (httpx.AsyncClient(app=...))
respx==0.21.1          # mock httpx calls in worker_router tests
pytest-cov==5.0.0      # coverage reports: pytest --cov=. --cov-report=term-missing
