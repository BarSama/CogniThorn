[pytest]
# Tell pytest-asyncio to treat every async test function as a coroutine
# automatically — no need to add @pytest.mark.asyncio to every single test.
asyncio_mode = auto

# Where to look for tests
testpaths = tests

# Minimum verbosity for CI readability
addopts = -v --tb=short
